#include<bits/stdc++.h>
using namespace std;

void permutateStr(string s, string ans){

    if(s.length() == 0){
        cout<<ans<<endl;
        return;
    }

    char ch = s[0];
    

    // ans  = ch + permutateStr(s.substr(1), ans);

}
